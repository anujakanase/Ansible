---
- name: Kill a process
  hosts: all

  tasks:
  - name: Find a process and kill it
    ignore_errors: yes
    shell: "pgrep mailx | xargs kill"

  - name: Start the service
    service:
      name: mailx
      state: started
